@font-face {
  font-family: 'heading';
  src: url('../font/heading.eot');
  src: url('../font/heading.eot?#iefix') format('embedded-opentype'),
       url('../font/heading.woff') format('woff'),
       url('../font/heading.ttf') format('truetype'),
       url('../font/heading.svg#heading') format('svg');
  font-weight: normal;
  font-style: normal;
}

/* General */

body {
  color: #415A68;
  line-height: 1.2rem;
}

p {
  margin-bottom: 1.5rem;
}

a {
  color: #006990;
}

.navbar {  
  background-image: linear-gradient(
    top,
    rgba(255,255,255,255) 0%,
    rgba(255,255,255,255) 90%,
    rgba(255,255,255,0) 100%
  );
  background-image: -o-linear-gradient(
    top,
    rgba(255,255,255,255) 0%,
    rgba(255,255,255,255) 90%,
    rgba(255,255,255,0) 100%
  );
  background-image: -moz-linear-gradient(
    top,
    rgba(255,255,255,255) 0%,
    rgba(255,255,255,255) 90%,
    rgba(255,255,255,0) 100%
  );
  background-image: -webkit-linear-gradient(
    top,
    rgba(255,255,255,255) 0%,
    rgba(255,255,255,255) 90%,
    rgba(255,255,255,0) 100%
  );
  background-image: -ms-linear-gradient((
    top,
    rgba(255,255,255,255) 0%,
    rgba(255,255,255,255) 90%,
    rgba(255,255,255,0) 100%
  );
}
 
.navbar [class*="span"] {
  margin-top: 1.5rem;
  margin-bottom: 1.5rem;
}

.hash { 
  height: 1px;
  width: 1px;
  display: block; 
}

.purple {
  color: #4D2177;
}

.green {
  color: #86A20B;
}

.blue {
  color: #006990;
}

/* Headings */

.heading {
  text-transform: uppercase;
  font-weight: normal;
  font-family: heading, sans-serif;
}

.subheading {
  margin-bottom: 0.5rem;
  font-weight: normal;
  font-family: heading, sans-serif;
}

h1 {
  font-size: 3.375rem;
  line-height: 2.7rem;
}

h2 {
  font-size: 2.25rem;
  line-height: 2rem;
}

/* Nav Section */

.nav {
  font-size: 1.5rem;
  line-height: 1.4rem;
}

.nav li {
  margin-bottom: 0.66rem;
}

/* Main Section */

.main .heading {
  margin: 5.0625rem 0 1.5rem;
}

/* Responsive */


/* fixed mode small header */
@media (min-width: 1200px) and (min-height: 400px) {
  body {
    padding-top: 8.5rem;
  }
  
  .hash {
    margin-top: -8.5rem;
    padding-bottom: 8.5rem;
  }
}


/* fixed mode big header */
@media (min-width: 768px) and (max-width: 1200px) and (min-height: 500px) {
  body {
    padding-top: 11.5rem;
  }
  
  .hash {
    margin-top: -11.5rem;
    padding-bottom: 11.5rem;
  }
}

/* fixed mode */
@media (min-width: 768px) and (min-height: 500px), (min-width: 1200px) and (min-height: 400px) {  
  .nav {
    position: fixed;
  }
}

/* static mode and single column mode */
@media (max-width: 767px), (max-height: 499px) and (max-width: 1199px), (max-height: 399px) {
  .nav-mock {
    display: none;
  }
  
  .navbar-fixed-top {
    position: static;
  }
}

@media (min-width: 979px) { 
  html, body {
    font-size: 20px;
  }
}

@media (max-width: 979px) {  
  html, body {
    font-size: 15px;
  }
}
